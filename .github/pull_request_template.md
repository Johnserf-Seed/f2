<!--
请确认本 PR 的目标分支（base）是当前开发分支，而不是 main。
当前开发分支以 README 顶部的 Dev Branch 徽章为准，目前为 v0.0.1.8-pw3。
Please make sure the base branch is the current development branch (currently v0.0.1.8-pw3), not main.
-->

## 改动说明 / Summary


## 关联 Issue / Related issues

Closes #

## 检查清单 / Checklist

- [ ] 目标分支是当前开发分支，不是 `main` / The base branch is the current development branch, not `main`
- [ ] 已添加或更新测试，`pytest -m "not network"` 通过 / Tests added or updated, `pytest -m "not network"` passes
- [ ] 已通过 `ruff`、`black`、`isort`、`mypy` 检查 / `ruff`, `black`, `isort` and `mypy` pass
- [ ] 行为变化已同步更新中英文档与 `CHANGELOG.md` / Behavior changes are reflected in the zh/en docs and `CHANGELOG.md`
- [ ] 没有提交真实的 cookie、token 或其他敏感信息 / No real cookies, tokens or other secrets are committed
